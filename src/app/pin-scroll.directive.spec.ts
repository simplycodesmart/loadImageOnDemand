import { PinScrollDirective } from './pin-scroll.directive';

describe('PinScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new PinScrollDirective();
    expect(directive).toBeTruthy();
  });
});
